# Inifile and its hash map

This is a third party library (MIT license) that provides R2DTWO with an INI file parser and a string-keyed hash table.

It also comes with unit tests in the *test* directory. To build and run the tests:

```
cd test
mkdir build
cd build
cmake ..
make
./hashmap
./inifile
```
