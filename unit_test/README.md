# Unit tests for R2DTWO

The tests use the same [infrastructure](https://gitlab.com/mtmkls/testing) as the INI parser library. To build and run the tests:

```
mkdir build
cd build
cmake ..
make
./test_time
```
